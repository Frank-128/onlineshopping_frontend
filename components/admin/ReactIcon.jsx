import React from 'react'
import { IconBase, IconContext } from 'react-icons'
import { TfiDashboard } from "react-icons/tfi";
function ReactIcon({icon,color,size=24}) {
  return (
    <TfiDashboard/>
  )
}

export default ReactIcon